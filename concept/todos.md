# Todos

- handle different types, e.g. dates, times, datetimes
- add globalFilters
  - add default filters
    - select, multiselect
    - range
    - date range
- move bootstrapUI to own package
- add fetcher
  - add sort
  - add filter
- add row select
